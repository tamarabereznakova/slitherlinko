package sk.tuke.gamestudio.game.slitherlink;

import sk.tuke.gamestudio.game.slitherlink.consoleUI.Lobby;

public class Slitherlink {
    //public static void main(String[] args) {
        //Generator generator = new Generator(1, 1);
        //Field field = generator.generate();

        //Field field = new Generator(0, 0).generateFromMap(PredefinedMaps.randomMap());

        /*
        System.out.println("=== POLE ===");
        System.out.println(field);
        System.out.println("=== ROSKY===");
        Tile t = field.getTile(3, 2);
        Edge[] edges = t.getEdges();
        for (int i = 0; i < 4; i++) {
            System.out.println("E " + i + ": " + (edges[i] != null ? "K" : "ERR"));
        }
        */
        //IDE TO


        /*
        System.out.println("=== VALIDATOR ===");
        //System.out.println("je vyhra? " + field.validator.validate(field)); //false
        field.checkWin();
        System.out.println("je vyhra? " + field.getState()); // PLAYING
        */
        //IDE TO

        //field.printDebug(3, 2); //IDE TO

        //new ConsoleUI(field).run();

        //new Lobby().start();
    //}
}